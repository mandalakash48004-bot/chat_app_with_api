# chat_app_with_api
A real-time Django chat app extended with a custom DRF API layer — supports user-to-user messaging and chat rooms, token/session-based API authentication, full CRUD on messages and conversations, and paginated message history. Tested all endpoints using Postman.
